var Opject1=[
    'Hely',
    'KhanhDuy',
    'Oke',
    'Output'
];
var newArray=Opject1.map(function(param)
{
    return param.length;
});
var newArray=Opject1.map((param)=>
{
return param.length
});
var newArray=Opject1.map(param=>param);
console.log(newArray);